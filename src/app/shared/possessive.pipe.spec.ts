import {PossessivePipe} from './possessive.pipe';

describe('PossessivePipe', () => {
  it('create an instance', () => {
    const pipe = new PossessivePipe();
    expect(pipe).toBeTruthy();
  });
});
